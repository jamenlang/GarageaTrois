dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "android-19,build-tools-19.0.1,extra-android-support"

  override:
    - chmod +x gradlew
    - ./gradlew dependencies

test:
  override:
    - ./gradlew assembleDebug
