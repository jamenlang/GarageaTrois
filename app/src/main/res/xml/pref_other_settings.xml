<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="debug"
        android:title="@string/pref_title_debug"
        android:summary="@string/pref_description_debug"
        android:defaultValue="false" />

    <CheckBoxPreference
        android:key="disablesettings"
        android:title="@string/pref_title_disablesettings"
        android:summary="@string/pref_description_disablesettings"
        android:defaultValue="false" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

</PreferenceScreen>
