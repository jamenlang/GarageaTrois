<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
    <CheckBoxPreference
        android:key="allow_nfc"
        android:title="@string/pref_title_nfc"
        android:summary="@string/pref_description_nfc"
        android:defaultValue="true" />
    -->
    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <EditTextPreference
        android:key="userresult"
        android:title="@string/pref_title_userresult"
        android:defaultValue="@string/pref_default_userresult"
        android:summary="Auth Token for Non-Admin Users"
        android:selectAllOnFocus="true"
        android:inputType="textPassword"
        android:capitalize="none"
        android:singleLine="true"
        android:maxLines="1" />

    <EditTextPreference
        android:key="adminresult"
        android:title="@string/pref_title_adminresult"
        android:defaultValue="@string/pref_default_adminresult"
        android:summary="Auth Token for Admin Users"
        android:selectAllOnFocus="true"
        android:inputType="textPassword"
        android:capitalize="none"
        android:singleLine="true"
        android:maxLines="1" />



    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

</PreferenceScreen>
